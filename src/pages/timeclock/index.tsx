import { PageLayout } from "~/components/layout";

// todo add clockPIN to userSettings

// pull all shifts for today
// show each user with their shifts for today, button to punch time card
// creates clock event

const ShiftFeed = () => {
  return <div>feed</div>;
};

export default function TimeClockPage() {
  return (
    <PageLayout>
      <ShiftFeed />
    </PageLayout>
  );
}
